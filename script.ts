import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); import * as bcrypt from 'bcryptjs'; async function main() { const email = 'wahab@alimam.com'; const password = 'Wahab26!'; const user = await prisma.profile.findFirst({ where: { email }, select: { id: true, email: true } }); if (user) { const hashed = await bcrypt.hash(password, 10); await prisma.profile.update({ where: { id: user.id }, data: { password_hash: hashed, role: 'admin_super' }, select: { id: true } }); console.log('User updated'); } else { const hashed = await bcrypt.hash(password, 10); await prisma.profile.create({ data: { email, phone: '081234567890', password_hash: hashed, full_name: 'Ust. Wahab', role: 'admin_super' }, select: { id: true } }); console.log('User created'); } } main().catch(console.error).finally(() => prisma.$disconnect());
