const fs = require('fs'); let c = fs.readFileSync('src/app/daftar/page.tsx', 'utf8'); c = c.replace(/} else if \\(formData\\.jenis_kelamin === "L" && formData\\.jenjang === "MTs"\\) {[\\s\\S]*?sudah ditutup\\.";\\n      }/g, ''); c = c.replace(/const isClosed = isPutra && \\(option\\.value === "MTs" \\|\\| option\\.value === "IL"\\);/g, 'const isClosed = false;'); fs.writeFileSync('src/app/daftar/page.tsx', c);
